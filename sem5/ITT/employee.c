#include<stdio.h>
int grosssalary(int a)
{
   return (basic+(0.8*basic)+(0.2*basic))
}
void deduction(int a,int b)
{
   return (
int main()
{
   printf("enter the name of the employee and his basic salary:");
   scanf("%d%d",&e,&b);
   printf("your gross salary is:",grosssalary(b))
}

